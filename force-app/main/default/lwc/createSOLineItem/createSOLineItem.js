import { LightningElement } from 'lwc';
export default class CreateSOLineItem extends LightningElement {
    myFields = ["AcctSeedERP__Comment__c"];

    handleLineItemCreated(){
        // Run code when account is created.
    }
}